server:
  port: 8082

spring:
  application:
    name: MEASUREMENT-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/quantitymeasurementapp
    username: root
    password: vanshriya123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,env
  endpoint:
    health:
      show-details: always
  info:
    env:
      enabled: true